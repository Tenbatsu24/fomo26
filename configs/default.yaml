model:
  model_variant: small
  patch_size: 14
  num_register_tokens: 4
  minibatch_size: -1
  lora: false

pretrained:
  checkpoint: /mnt/c/Users/puruv/Projects/fomo26/models/small/neco/encoder_teacher.ckpt

data:
  dataset_name: CLS002_FOMO26_Infarct
  crop_size: [128, 128, 16]
  batch_size: 8
  num_workers: 8

optim:
  lr: 0.0001
  weight_decay: 0.05
  betas: [0.9, 0.999]
  total_steps: null

trainer:
  max_steps: 100000
  precision: 32-true
  devices: auto
  strategy: auto
  log_every_n_steps: 50
  gradient_clip_val: 3.0
