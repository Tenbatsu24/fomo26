Bootstrap: docker
From: puruvaish24/pytorch-xformers:2.10-cu126

%labels
    Author FOMO26 Challenge - Brain Age Regression Task
    Version v1.0.0
    Description FOMO26 Brain Age Prediction Apptainer Image

%environment
    export PYTHONUNBUFFERED=1
    export LC_ALL=C.UTF-8

%files
    # Copy your files (model, predict.py, requirements.txt, ...) to the container
    ./predict.py /app/predict.py
    ./requirements.txt /app/requirements.txt
    ./src /app/src
    ./setup.py /app/setup.py
    ./models /app/models
    ./pyproject.toml /app/pyproject.toml

%post
    set -eux

    cd /app
    python -m pip install --no-cache-dir --break-system-packages .
    cd /

    chmod +x /app/predict.py

%runscript
    # Execute the prediction script with all arguments
    exec python /app/predict.py "$@"

%help
    This container runs the FOMO26 brain age prediction task.

    Build this container with:

        `apptainer build --fakeroot /path/to/save/brain_age.sif /path/to/Apptainer.def`

    Usage:

        `apptainer run --bind /path/to/input:/input:ro \
            --bind /path/to/output:/output \
            --nv \
            brain_age.sif \
            --t1 /input/t1.nii.gz \
            --output /output/age_prediction.txt`
